import React from 'react';
import '../App.scss'

const Modal = () => {
    return (
        <div>
            <button className="modal">Hourly Forecast</button>
        </div>
    );
};

export default Modal;